# Example environment variables for Docker deployment
# Copy this file to .env and customize as needed

# OpenAI API Key (alternative to setting in config.yaml)
OPENAI_API_KEY=your_openai_api_key_here

# Timezone for container
TZ=UTC

# Optional: Directory for storing logs (defaults to 'logs')
# EMAILFILTER_LOGS_DIR=logs

# Optional: Directory for storing state (defaults to '~/.emailfilter')
# EMAILFILTER_STATE_DIR=~/.emailfilter
